{
  "name": "blackboard-js",
  "version": "1.0.0",
  "description": "",
  "files": [
    "dist"
  ],
  "publishConfig": {
    "access": "public"
  },
  "bin": "dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "tsc --watch --preserveWatchOutput",
    "prepublishOnly": "esbuild src/index.ts --platform=node --outfile=dist/index.js --bundle --minify --define:process.env.NODE_ENV=\\\"production\\\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
   "@blackboard-js/local-client": "^1.0.0"
  },
  "devDependencies": {
    "@types/node": "^14.14.31",
    "esbuild": "0.8.26",
    "typescript": "^4.2.3",
    "@blackboard-js/local-api": "^1.0.0",
    "commander": "^7.1.0"
  }
}
